[package]
name = "agent-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
trading-core = { path = "../core" }
async-trait = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
chrono = { workspace = true }
broker-paper = { path = "../broker-paper" }
